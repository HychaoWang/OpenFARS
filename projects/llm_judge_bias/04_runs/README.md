# Runs

- Place execution configs in `config_*.json` and logs under `logs/`.
- Each run should record seed, prompt versions, dataset slice, and token usage.
- Keep CSV/JSON metrics per run; aggregated summaries go to `summary.md`.
